import {StyleSheet} from 'react-native';

export const tabStyles = StyleSheet.create({
  tabTxt: {
    fontSize: 12,
    color: '#424242',
    fontWeight: '500',
  },

  focused: {
    color: 'white',
  },
});
